(function() {
  var $;

  $ = jQuery;

  var $cssEditors = $('.type-css textarea');

  $cssEditors.each(function() {

    var editor = new Behave({
      "textarea": this
    });

    $(this).data('editor', editor);

  });

}).call(this);
